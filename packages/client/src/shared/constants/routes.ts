export enum ROUTES {
  Login = '/login',
  Main = '/',
  SignUp = '/sign-up',
  Profile = '/profile',
  LeaderBoard = '/leaderboard',
  OAuth = '/oauth',
  NotFound = '*',
  Forums = '/forums',
  Topics = '/forums/:forumId',
  Topic = '/forums/:forumId/:topicId',
}
